/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["flowbite.s3.amazonaws.com"], // Add any other domains you're using images from
  },
};

module.exports = nextConfig;
